# Codex review — M4 stock-hub module spec (2026-07-21)

> Channel record per docs/redesign-coordination.md §1.3 — verdict verbatim from
> `codex exec` (review-only run, no files modified). Spec under review:
> `docs/redesign-m4-stock-hub-spec.md`. Dispositions: docs/redesign-coordination.md §3b.

VERDICT: APPROVE-WITH-CHANGES

1. BLOCKING: §7 overstates the "existing panel functions" reuse claim for pt14. The cited MEP, fingerprint, seasonal, momentum, and CPR pieces exist: `src/web/dashboard.py:2316` `_mep_stock_panel`, `src/web/credibility_fingerprint.py:212` `card_html`, `src/web/seasonal_view.py:1003` `seasonal_full_panel`, `src/web/momentum_pane.py:291` `card_html`, and `src/web/dashboard.py:2204` `_cpr_stock_panel`. But pt14 is inline HTML built inside `dash_stock`, not an existing panel function: `src/web/dashboard.py:6355`, `src/web/dashboard.py:6367`, `src/web/dashboard.py:6391`. Because `src/web/dashboard.py` is frozen for ordinary feature work (`AGENTS.md:105`), the spec must say the v3 hub will add a new pt14 renderer/wrapper in new files, not "thin wrappers over existing panel functions" for pt14.

2. ADVISORY: §6/§2 misnames `sector_peers` as if it were a real table/function. The current code has a local `sector_peers` variable in the legacy stock dossier (`src/web/dashboard.py:6589`, `src/web/dashboard.py:6591`, `src/web/dashboard.py:6679`) and a `/dash/api/peers` endpoint (`src/web/symbol_search.py:177`, `src/web/symbol_search.py:178`) backed by `_sector_symbols` (`src/web/dashboard.py:980`), not a `sector_peers` table or reusable function. Data source is real, name should be corrected.

3. ADVISORY: The chart overlay seams are real, and wrapping is viable only if the wrapper preserves the same DOM/global contract. `window.__wfpc` and `window.__wfcandle` are exposed at `src/web/stock_chart.py:108` and `src/web/stock_chart.py:110`; `[data-ptf]` is used by the chart and CPR overlay at `src/web/stock_chart.py:543` and `src/web/cpr_overlay.py:119`; `#stratBar` and `#cprBar` are created at `src/web/stock_chart.py:442` and `src/web/stock_chart.py:455`. Existing overlays bind to those seams (`src/web/cpr_overlay.py:110`, `src/web/mep_overlay.py:86`, `src/web/indicators_overlay.py:49`, `src/web/wolfe_overlay.py:48`). The dense rail must re-home rather than rename/remove them.

4. ADVISORY: §3's comparison work is more than visual wrapping. The legacy chart has compare state and a cap of 4 (`src/web/stock_chart.py:295`, `src/web/stock_chart.py:694`, `src/web/stock_chart.py:695`) plus exposed add/remove hooks (`src/web/stock_chart.py:756`), but it does not initialize from `?cmp=` or write compare changes via `history.replaceState`; the only `replaceState` hit nearby is tab hash state in the legacy dossier (`src/web/dashboard.py:6909`). The spec's new `chart_rail_v3.py` URL-state shim is therefore required and should be tested explicitly.

5. ADVISORY: The remaining data reads and gates check out. Real tables/functions exist for `stock_signals`, `mep_signals`, `fno_oi_signals`, `pattern_scores`, `cpr_signals`, `concall_scores`, `corporate_actions` in `src/core/db.py:151`, `src/core/db.py:294`, `src/core/db.py:333`, `src/core/db.py:372`, `src/core/db.py:448`, `src/core/db.py:613`, `src/core/db.py:869`; `capital_allocation_scores` is module-owned at `src/automation/capital_allocation.py:326`; `board_meetings` is owned at `src/automation/results_calendar.py:41`; news and seasonal embeds exist at `src/web/news_view.py:106` and `src/web/dashboard.py:6776`. `_ROUTER_SPECS` mounting is real (`src/web/v2_surfaces.py:42`, `src/web/v2_surfaces.py:302`), route classification requires `INTERNAL_DEV` registration (`tests/test_dash_route_registry.py:93`, `tests/test_dash_route_registry.py:177`), and Pat coverage derives only routed lenses from `lens_registry` (`tests/test_pat_coverage.py:65`, `tests/test_pat_coverage.py:67`), so §1's "Pat coverage is unaffected" is correct if `/dash/preview/stock` stays `INTERNAL_DEV` and not a lens.

6. ADVISORY: The pt14 quality-gate numbers are independently verified. `QG_MAX` is 252 and `QG_THRESHOLD` is 151.2 at `src/automation/scoring.py:60`, `src/automation/scoring.py:61`, `src/automation/scoring.py:62`; unverified signals use `UNVERIFIED_MULTIPLIER = 0.70` at `src/automation/scoring.py:64`, applied at `src/automation/scoring.py:354` and `src/automation/scoring.py:624`.

7. ADVISORY: No additional verdict/advice violation found in §2/§4/§5 beyond the already-recorded narrative risk. The coordination record explicitly requires representative narrative-template tests and shared fence vocabulary (`docs/redesign-coordination.md:58`), while the standing doctrine says the product "never tells you what to buy" (`docs/redesign-plan-2026-07-17.md:338`) and forbids weakening the descriptive fence.
