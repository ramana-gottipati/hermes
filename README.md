# Hermes

Personal AI agent on a VPS — assistant + automation + trading/finance, in one Python codebase.

See [CLAUDE.md](CLAUDE.md) for full orientation.

## Quick start

```bash
cp .env.example .env       # fill in ANTHROPIC_API_KEY
docker compose up --build
```

API lands on `http://localhost:8000`.
