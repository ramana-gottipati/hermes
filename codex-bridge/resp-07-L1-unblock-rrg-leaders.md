I’m blocked before I can read `codex-bridge\req-07-L1-unblock-rrg-leaders.md`.

Every shell command is failing at sandbox startup, including `Get-Location` with no working directory:

```text
windows sandbox: spawn setup refresh
```

Because I can’t read `PROJECT_STATE.md`, `git log`, or the bridge request, I can’t safely follow the repo’s required boot procedure or the request “exactly.” Please retry the session/tool environment, or paste the contents of `PROJECT_STATE.md` and `codex-bridge\req-07-L1-unblock-rrg-leaders.md` here so I can proceed from the provided text.