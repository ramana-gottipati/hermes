I could not read `codex-bridge/req-09-docs-currency-verify.md` because every read-only shell invocation failed before PowerShell started:

`windows sandbox: spawn setup refresh`

I also checked MCP resources, and none are exposed for this workspace. No files were modified, staged, or deleted.