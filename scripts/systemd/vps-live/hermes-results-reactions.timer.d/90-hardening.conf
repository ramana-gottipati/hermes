# Persistent=false (base .timer): a snapshot needs no boot catch-up — the next fire refreshes it —
# and this makes enabling mid-day AUD-95-safe (no missed-elapse catch-up fire). RandomizedDelaySec
# spreads the 18:00 fire off the exact minute, clear of the ingest cluster tail.
[Timer]
RandomizedDelaySec=600
