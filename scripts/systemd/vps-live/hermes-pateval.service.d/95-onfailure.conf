# AUD-40 + AUD-26: a Pat routing/explain/hallucination/accuracy regression fails the gate →
# page Ramana (Telegram DM via hermes-alert@). The DM names the unit; read which evals failed
# in /var/log/hermes-pateval.log.
[Unit]
OnFailure=hermes-alert@%n.service
