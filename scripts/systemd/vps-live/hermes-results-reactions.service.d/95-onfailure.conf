# AUD-26: page on failure (Telegram DM via hermes-alert@). Season-critical unit.
# AUD-29: order after the bhav fetch on the rare occasion both are queued together —
# the in-code refuse-if-stale gate (pead_surface._tape_freshness) is the real defense.
[Unit]
OnFailure=hermes-alert@%n.service
After=hermes-bhavcopy.service
