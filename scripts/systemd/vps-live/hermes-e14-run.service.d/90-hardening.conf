# AUD-31/35 hardening drop-in (house pattern). 1h ceiling: reconciliation abort takes
# seconds; the full protocol (3 combos x CAR + 200-shuffle placebo each) fits inside.
[Service]
TimeoutStartSec=3600
NoNewPrivileges=yes
PrivateTmp=yes
ProtectHome=yes
ProtectSystem=strict
ReadWritePaths=/opt/hermes /var/log
