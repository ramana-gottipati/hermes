# AUD-31/35 hardening drop-in (fleet-standard, S77b pattern). Read-only job;
# 10 min is generous (the registry read takes <1s warm).
[Service]
TimeoutStartSec=600
NoNewPrivileges=yes
PrivateTmp=yes
ProtectHome=yes
ProtectSystem=strict
ReadWritePaths=/opt/hermes /var/log
