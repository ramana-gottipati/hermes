# AUD-26: page on failure (Telegram DM via hermes-alert@). This exact feed once died
# SILENTLY for ~6 months (archive-CSV 404s, nothing watching) — failure must page.
# data_quality.chk_derived_liveness independently WARNs if the table stops growing.
[Unit]
OnFailure=hermes-alert@%n.service
