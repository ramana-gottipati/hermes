[Service]
# Accumulation x fundamental-quality screen (D66): runs after the signal chain
# (10-signals.conf builds stock_signals; needs the day's bhavcopy_rows for price).
# Numpy-free -> runs under the prod venv. Isolated drop-in (lexical order 10..50,
# so it runs LAST; a failure here cannot break the upstream signal chain).
ExecStart=/opt/hermes/.venv/bin/python -m src.automation.accum_screen
