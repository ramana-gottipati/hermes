# S84: this unit is a full-universe re-collect (2767 symbols @ ~25/min ≈ 110
# min) — the fleet-wide TimeoutStartSec=1800 in 90-hardening.conf SIGTERM'd it
# mid-run on 2026-07-07 (killed at 675/2767, marked failed, paged) and would
# again every run. 4h keeps the wedge-guard finite (the hardening intent) while
# letting the real job finish. Ordered 99- so it wins over 90-hardening.conf.
[Service]
TimeoutStartSec=14400
