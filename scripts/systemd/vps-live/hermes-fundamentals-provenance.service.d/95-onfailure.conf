# AUD-26: page on failure (Telegram DM via hermes-alert@). Season-critical unit.
# (This unit crashed silently on 2026-07-04 — the 7-col INSERT — and nobody knew for
# three days. This drop-in is why that never happens again.)
[Unit]
OnFailure=hermes-alert@%n.service
